# AATE v1 course archive

This directory preserves the instructional surface that existed immediately
before the source-first rewrite began on 2026-07-15.

- `COURSE.md` is the former monolithic course source.
- `COURSE.html` is the corresponding strict MkDocs render.
- `index.html` is the former rendered landing page.
- `mkdocs.yml` records the former five-item navigation.

This archive is historical evidence only. It is intentionally excluded from
the learner navigation and is not an alternate course path.
